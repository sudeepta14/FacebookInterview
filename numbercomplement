class Solution {
    public int findComplement(int num) {
        int sum = 1, pow = 1;
        while(sum < num){
            pow *= 2;
            sum += pow;
        }
        return sum - num;
    }
}
